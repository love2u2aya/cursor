# Google Cloud OAuth credentials file path (relative to project root)
CLIENT_SECRETS_FILE=client_secret.json

# OAuth token storage path
TOKEN_FILE=token.json

# Default report output directory
REPORT_OUTPUT_DIR=reports

# Default analysis period in days
DEFAULT_ANALYSIS_DAYS=90

# Default max videos to analyze
DEFAULT_MAX_VIDEOS=50
